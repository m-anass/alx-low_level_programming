#include "main.h"
/**
 * _islower - entry point
 * Descriotion: A C program that pritns put function
 * @c: the char to check
 * Return: Alawys 0 (Success)
 */
int _islower(int c)
{ 
	if (c >= 97)
		return (1);
	else
		return (0);
}
